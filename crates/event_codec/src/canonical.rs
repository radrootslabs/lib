//! Canonical event representation and normalization boundary.

/// Internal marker for the target module while canonical transforms are consolidated.
#[allow(dead_code)]
pub(crate) struct ModuleScaffold;
