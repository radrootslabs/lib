//! Canonical event-tag types.
//!
//! Existing tag behavior moves here during module normalization.

/// Internal marker for the target module while tag types are migrated.
#[allow(dead_code)]
pub(crate) struct ModuleScaffold;
