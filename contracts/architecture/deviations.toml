schema_version = 1
architecture_id = "radroots.crates.release.v1"

[[deviation]]
id = "RCRV1-DEV-017"
date = "2026-09-10"
status = "closed"
approval = "Explicit user authorization covers necessary shared-owner prerequisites, verified commits and non-force source publication."
affected_steps = ["157", "174", "204"]
spec_anchors = [
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_storage",
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_sync",
]
source_evidence = [
  "Shared sync rejects contract-invalid signed observations before the host can choose verified-only retention.",
  "Shared visibility selects replacement heads only from Visible records, allowing an older visible payload to survive a newer retained signature-verified head.",
]
replacement_action = "Implement contracts/architecture/decisions/verified_event_heads.v1.json: explicit default-reject verified retention and canonical head selection before payload visibility, without adding product policy to shared storage."
verification = [
  "Reproduce stale fallback and verify raw, malformed, signature, tie, author, deletion-before-target, order, stage advancement and memory/SQLite parity cases.",
  "Review additive public API and pass affected owner and SDK tests, unchanged coverage thresholds, workspace contracts and release preflight.",
]
unresolved_risk = "Owner, backend, SDK, additive API, unchanged coverage and workspace qualification pass. Verified heads intentionally suppress older visible payloads; only opt-in policies retain contract-invalid signed observations. No external release or deferred platform qualification is claimed."
normative_architecture_change = false
adr_required = false
closure_evidence = [
  "The original stale-fallback regression now passes with raw-to-verified advancement, canonical ties, empty coordinates, reversed arrival order and author-authorized deletion-before-target.",
  "Real signed sync ingestion preserves default rejection and rejects bad signatures before host retention; opt-in failures stay Verified and valid later admission advances at the same raw count.",
  "Memory and reopened SQLite return matching admission receipts, visibility snapshots and bounded queries. All isolated owner and SDK lanes pass; only the defaulted retention method and closed decision are additive API changes.",
  "Fresh storage, SQLite, sync and SDK coverage and all 45 required reports/aggregate pass with unchanged thresholds, together with complete workspace, portable and release-preflight gates.",
]

[[deviation]]
id = "RCRV1-DEV-016"
date = "2026-09-10"
status = "closed"
approval = "Standing user authorization covers necessary shared-owner repairs, verified checkpoint commits and non-force producer publication."
affected_steps = ["201"]
spec_anchors = [
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_transport_nostr",
]
source_evidence = [
  "The concrete REQ asks for 1000 events, but exactly 1000 followed by EOSE is currently normalized as Complete.",
  "After a 500-event first page, another 500 equal-time events produce Complete with no cursor despite additional capped or older history.",
]
replacement_action = "Implement contracts/architecture/decisions/nostr_fetch_windows.v1.json within the existing adapter: partial capped coverage, lossless paging of received candidates and finite explicit older-window continuation."
verification = [
  "Reproduce capped EOSE false-completion, then verify ordinary and capped ties, duplicate relays, older history, malformed/boundary inputs and scoped continuation through injected and loopback sources.",
  "Pass affected package and SDK checks, unchanged public API and coverage thresholds, full workspace and required release-preflight gates before publication.",
]
unresolved_risk = "Current owner, SDK, API, coverage and workspace gates pass. A saturated timestamp can still hide additional events; the partial yield and explicit older continuation never claim lossless or global-history recovery."
normative_architecture_change = false
adr_required = false
closure_evidence = [
  "The false-complete regression now passes with 501, 1000 and 1001 equal-time events, cross-relay deduplication, explicit older continuation and canonical scoped cursor rejection.",
  "A real WebSocket relay returns exactly 1000 signed same-time events and EOSE; both received pages and the explicit older query pass without reconnect backoff or automatic boundary skipping.",
  "Malformed/out-of-bound and zero-time cases fail closed. Public transport and SDK APIs remain byte-identical. Fresh affected coverage, all 45 reports and aggregate, complete workspace and release-preflight checks pass without threshold changes.",
]

[[deviation]]
id = "RCRV1-DEV-015"
date = "2026-09-09"
status = "closed"
approval = "Standing user approval of the reviewed refactor and necessary shared-owner prerequisites, including verified producer commits and non-force publication."
affected_steps = ["201"]
spec_anchors = [
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_sync",
]
source_evidence = [
  "PullReceipt intentionally retains the final per-target outcome, so a later complete page replaces an earlier partial or failed page.",
  "FetchPage permits missing target outcomes; a complete page marker alone cannot prove every requested target supplied complete evidence.",
]
replacement_action = "Add bounded cumulative per-target summaries under contracts/architecture/decisions/pull_target_evidence.v1.json, preserving final-page API semantics and legacy receipts as unknown evidence."
verification = [
  "Exercise incomplete/complete ordering, omitted outcomes, multiple targets, exact limits, termination and compatible serialization through the real shared engine and SDK delegation.",
  "Qualify current package/workspace, API, feature, coverage and release-preflight surfaces before publication and consumer adoption.",
]
unresolved_risk = "Current shared sync, SDK, workspace, API and unchanged coverage gates pass. Summary completeness remains scoped to returned pages and must be combined with pull termination; no global-history or new release authority is claimed."
normative_architecture_change = false
adr_required = false
closure_evidence = [
  "The reproduced partial-then-complete regression passes while final outcomes remain unchanged; all actual incomplete states, missing outcomes, multiple targets, 64 targets and 1,000 pages are exercised.",
  "Legacy and malformed serialization, source/page/deadline/cancellation cases, and actual SDK cumulative-evidence delegation pass with the supported feature profiles.",
  "Generated shared API retains every previous declaration, SDK API is byte-identical, fresh sync/SDK coverage and all 45 reports/aggregate pass, and the full workspace and release-preflight lanes pass.",
]

[[deviation]]
id = "RCRV1-DEV-013"
date = "2026-09-09"
status = "closed"
approval = "Standing user approval of the complete reviewed refactor and its necessary same-owner producer repairs."
affected_steps = ["201"]
spec_anchors = [
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_transport_nostr",
]
source_evidence = [
  "Current source.rs starts each buffered relay batch with a fresh timeout although the public fetch contract promises one absolute request deadline.",
  "Per-relay event counts are bounded, but aggregate raw JSON bytes and parse inventory are not explicitly capped before shared candidate collection; relay.rs inherits dependency-default wire limits.",
]
replacement_action = "Repair the current concrete adapter within its existing bounded-fetch charter, using contracts/architecture/decisions/nostr_fetch_bounds.v1.json. This is a current source repair, not a reopening or replacement of historical release qualification."
verification = [
  "Deterministic limit, aggregate competition, queued-deadline and retained partial-outcome regressions plus bounded loopback source tests.",
  "Canonical package/workspace checks, contracts, architecture, public API review and the unchanged affected-package coverage requirement.",
]
unresolved_risk = "Current package and workspace verification, byte-identical public API, all 49 required coverage reports and release preflight pass on the qualified source. Per-target partial/cancelled evidence is bounded; no global-history, power-loss, external release, device or new-platform qualification is claimed."
normative_architecture_change = false
adr_required = false
closure_evidence = [
  "Fresh transport coverage passes all four unchanged thresholds; the public API is byte-identical to its baseline.",
  "All 49 required coverage reports and the aggregate pass, together with workspace check, tests, Clippy, Rustdoc, contracts, freshness, dependency graph, portable checks and release preflight.",
  "Deterministic maxima and aggregate budget tests plus real loopback queued-deadline, mixed-relay, oversized-wire and cancellation regressions pass.",
]

[[deviation]]
id = "RCRV1-DEV-014"
date = "2026-09-09"
status = "closed"
approval = "Explicit user authorization to repair required repository coverage and release-preflight blockers while preserving quality thresholds and deferred qualification scope."
affected_steps = ["315"]
spec_anchors = [
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#quality_policy.coverage",
]
source_evidence = [
  "Current release preflight requires all current coverage-policy package reports and their aggregate; the aggregate is absent and the existing xtask line/branch measurements are below policy.",
  "The advisory, bounded-process and safe-artifact contracts name executable self-tests, but Cargo tests check their decision metadata without executing the complete CLI self-test suites.",
  "The detailed coverage scope mapper assumes crates/<package>/src even for xtask, whose source is tools/xtask/src; this selects no detailed functions or regions and reports empty perfect percentages.",
  "A deterministic malformed RustSec affected-object regression reproduces a panic when the functions member is absent; checked optional lookup must return the existing InvalidReport error instead.",
  "Workspace-only coverage cleanup leaves orphan xtask executables under debug/build/xtask with older source maps. A measured one-line parser shift adds stale branch/region records; reset the owned coverage build tree before current collection instead of filtering those records or trusting contaminated metrics.",
  "Retained gate and platform validators lack isolated argument, canonical-authority, inventory and result-encoding coverage. Extract those private transformations without changing pinned historical contracts or invoking their external qualification commands.",
  "Synthetic parsed ELF fixtures reproduce acceptance of 32-bit and big-endian binaries despite the existing elf64_little_endian_x86_64_execute_or_pie contract. Enforce the existing class and byte-order requirements alongside machine, entrypoint and executable-segment validation.",
  "Normalized Gradle graph validation is coupled to descriptor-bound admission that rejects changed projections before their structural checks. Isolate the existing private structural validator, preserve its call order, and exercise malformed graph identity, lineage and artifact bindings without fabricating admitted filesystem evidence.",
  "Gradle projection metadata and process-receipt bindings share the same admitted-projection short circuit as graph structure. Isolate their private validator without changing check order or errors so receipt, raw-source, count and normalization bindings can be tested with structurally valid inputs.",
  "Fresh collection succeeds for all 49 required packages, with 47 passing every coverage gate. Runtime paths has 82.407407 percent branch coverage and service SQLite has 89.316239 percent; their other metrics pass. Add focused owner tests for retained directory identity and cleanup, bounded schema catalogs, metadata and migration-history rejection without changing public behavior, policy or historical qualification.",
]
replacement_action = "Qualify current required-package behavior through the governed coverage commands, execute omitted existing CLI self-tests, and add narrow owner tests or testability repairs justified by actual uncovered paths. This repairs current qualification and does not reopen historical release or deferred Nix/device gates."
verification = [
  "Current owned command self-tests and their failure assertions execute from Cargo integration tests with no external provider or Nix access.",
  "All currently required package reports retain the existing coverage policy; the generated aggregate and release preflight must pass before qualification is complete.",
  "Relevant Rust, contract, architecture, API, feature and generated-freshness checks remain required.",
]
unresolved_risk = "Current macOS aarch64 qualification is complete. Deferred Nix, device, historical release and deployment qualification remain outside this repair; two unchanged opt-in SDK generators are not claimed by the workspace test lane."
normative_architecture_change = false
adr_required = false
closure_evidence = [
  "All 49 required package reports pass the unchanged four-metric coverage policy, and the governed aggregate and release preflight pass.",
  "The full workspace check, test, Clippy and Rustdoc lanes pass, together with catalog, architecture, contracts, DTO freshness, API boundaries, resolved dependency graph and portable checks.",
  "Runtime-paths and service-SQLite documentation tests pass; generated public API text is byte-identical to each reviewed baseline. Their five changed source files contain test-only additions.",
  "Cargo tests execute the three complete offline command self-tests. The workspace reports 3816 passing tests and eight ignored entrypoints: six process children exercised by parents and two unchanged opt-in SDK generators outside this lane.",
]

[[deviation]]
id = "RCRV1-DEV-001"
date = "2026-07-27"
status = "active"
approval = "Explicit user correction dated 2026-07-27."
affected_steps = [
  "015",
  "016",
  "017",
  "018",
  "019",
  "020",
  "021",
  "022",
  "023",
  "026",
]
spec_anchors = [
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#repositories.lib",
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#repository_policy",
]
source_evidence = [
  "The final v1 specification allocates 17 public packages to radrootslabs/lib and 2 to radrootslabs/sdk.",
  "Both existing repositories have independent histories, workspaces, lockfiles, remotes, and standalone release boundaries.",
]
replacement_action = "Retain the two existing standalone repositories; replace import and monorepo-unification work with independent workspace, lockfile, metadata, dependency, and release qualification."
verification = [
  "Both repository-local architecture validators resolve every spec anchor.",
  "The synchronized architecture catalog enforces the exact 17/2 ownership partition.",
  "Each standalone repository owns a required architecture CI adapter over its repository-local command surface.",
]
unresolved_risk = "Parent gitlinks cannot advance until the new standalone commits are public-remote reachable under separate authorization."
normative_architecture_change = false
adr_required = false

[[deviation]]
id = "RCRV1-DEV-010"
date = "2026-08-03"
status = "closed"
approval = "All code-review recommendations and the full multi-RCLD implementation sequence were explicitly approved by the user."
affected_steps = ["201", "215", "269", "294", "301", "313"]
spec_anchors = [
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_transport_nostr",
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_transport",
]
source_evidence = [
  "The Step 294 downstream matrix proved the former app_rt radroots_net_core consumer had migrated and no current first-party manifest retained an edge to radroots_net.",
  "Step 301 full workspace qualification proved that the assigned radroots_nostr_runtime Step 215 deletion and radroots_net Step 313 deletion had not been applied even though both removal gates were satisfied.",
  "Both obsolete packages failed against the final transport boundary when the complete all-feature workspace closure was checked.",
]
replacement_action = "Delete radroots_nostr_runtime, its NostrDB runtime adapter, and radroots_net during Step 301 qualification; forbid their package, dependency, alias, feature, and source identities from being reintroduced."
verification = [
  "Transport-owned tests reject reintroduction of radroots_nostr_runtime, radroots_net, or the NostrDB runtime-adapter feature.",
  "Workspace-wide source tests reject every removed transport-client identifier without a compatibility exception.",
  "The full all-target and all-feature workspace qualification compiles without either predecessor package.",
]
unresolved_risk = "None for the two removed packages; historical names remain only in governed specifications, migration evidence, and fail-closed regression assertions."
normative_architecture_change = false
adr_required = false
closure_evidence = [
  "crates/transport_nostr/tests/workspace_consumers.rs proves both package directories and workspace dependency identities are absent.",
  "crates/transport_nostr/tests/legacy_quarantine.rs proves release policy and the compatibility ledger no longer classify either package as a retained shim.",
]

[[deviation]]
id = "RCRV1-DEV-008"
date = "2026-08-01"
status = "closed"
approval = "All code-review recommendations and the full multi-RCLD implementation sequence were explicitly approved by the user."
affected_steps = ["153", "155", "171", "179", "226", "288", "293", "313"]
spec_anchors = [
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_secrets",
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_storage_sqlite",
]
source_evidence = [
  "The final radroots_storage_sqlite scaffold can consume radroots_secrets immediately and has no predecessor secret dependency.",
  "Step 179, not Step 153, owns transfer of the current SDK private database and its encrypted records into canonical SQLite storage.",
  "Mixed publish-frozen runtime, Nostr-account, SimpleX preview, SDK private-store, Myc, and other external hosts still require predecessor vault/store behavior until their ordered migration steps.",
]
replacement_action = "Activate the final radroots_storage_sqlite and SDK dependency edges in Step 153; confine predecessor vault/store imports to exact publish-frozen quarantine packages and the SDK private-store module; Step 179 transfers canonical private storage, Steps 226/288/293 migrate the remaining SDK and downstream consumers, and Step 313 removes every remaining compatibility package and legacy name."
verification = [
  "Consumer-migration tests enumerate every lib package manifest that still names radroots_secret_vault or radroots_protected_store and reject any unapproved or publishable consumer.",
  "Storage SQLite package-boundary tests require radroots_secrets and reject all predecessor secret package names.",
  "SDK source-boundary tests confine predecessor imports to private_store.rs and require the final optional radroots_secrets dependency edge.",
  "Step 155 release-policy validation keeps every quarantine package non-publishable until its exact removal gate.",
]
unresolved_risk = "None; the predecessor packages and all active manifest consumers are absent."
normative_architecture_change = false
adr_required = false
closure_evidence = [
  "crates/secrets/tests/consumer_migration.rs rejects every predecessor package directory, workspace dependency, and active manifest reference.",
  "Step 313 source census confirmed the SDK, Myc, CLI, and library runtime paths use only radroots_secrets and final storage owners.",
]

[[deviation]]
id = "RCRV1-DEV-007"
date = "2026-07-30"
status = "closed"
approval = "All code-review recommendations and the full multi-RCLD implementation sequence were explicitly approved by the user."
affected_steps = ["122", "170", "215", "235", "305"]
spec_anchors = [
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_transport",
]
source_evidence = [
  "Step 120 migrated canonical adapters and registries to independent EventSource and EventSink contracts.",
  "Mixed runtime delivery workers still consume predecessor request and receipt models until their ordered RCLD 40 migration.",
  "The runtime delivery worker also accepts opaque byte payloads, while the final EventSink contract accepts only verified SignedEvent values; deleting the bridge at Step 170 would silently remove a supported private runtime path before sync orchestration owns that decision.",
  "The standalone publish-frozen SDK still maps user-facing target and satisfaction models into predecessor outbox orchestration until Step 235.",
  "oss/cli/src/runtime/{config,sync,transport}.rs and oss/radrootsd/src/core/transport_publish.rs still import the predecessor identity aliases and Reticulum target helpers; those standalone repositories are outside the approved crate-surface mutation scope.",
]
replacement_action = "Remove the monolithic trait from radroots_transport in Step 122; retain one explicitly named runtime-owned unpublished shim until Step 215 at the sync-orchestration retirement gate, the SDK-local unpublished target/satisfaction mapping until Step 235, and documentation-hidden external-consumer aliases/helpers until the fail-closed package-realistic Step 305 gate."
verification = [
  "Transport source-boundary tests reject every removed public predecessor name and require the singular runtime-owned shim.",
  "Release policy keeps runtime and SDK publication disabled while either downstream shim exists.",
  "Steps 215 and 235 are the exact fail-closed final-removal gates for the remaining runtime and SDK mappings.",
  "Step 305 rejects publication until oss/cli and oss/radrootsd no longer require the documentation-hidden external-consumer aliases and Reticulum helpers.",
]
unresolved_risk = "None; final consumers use TransportId, Target, TargetScope, TargetLabel, and TargetFingerprint directly."
normative_architecture_change = false
adr_required = false
closure_evidence = [
  "crates/transport/tests/package_boundary.rs rejects the hidden Reticulum constructors, legacy constant spellings, and prefixed target aliases.",
  "Step 313 source census confirmed every external-consumer alias and helper is absent after the daemon cutover to final transport identities.",
]

[[deviation]]
id = "RCRV1-DEV-009"
date = "2026-08-02"
status = "closed"
approval = "All code-review recommendations and the full multi-RCLD implementation sequence were explicitly approved by the user."
affected_steps = [
  "170",
  "179",
  "189",
  "196",
  "201",
  "213",
  "226",
  "235",
  "263",
  "269",
  "288",
  "292",
  "313",
]
spec_anchors = [
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_storage",
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_storage_sqlite",
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_sdk",
]
source_evidence = [
  "The Step 170 first-party census found radroots_event_index consumers in the standalone CLI, SDK, SDK bindings, and indexer repositories.",
  "The census found radroots_event_store and radroots_outbox consumers in the standalone CLI and SDK, plus active library transport_nostr orchestration scheduled to move in Step 196.",
  "The census found radroots_runtime_store consumed by the standalone CLI, whose crate cutover is scheduled after the canonical library storage and sync implementations are complete.",
  "Deleting these packages before their ordered migrations would make the independently buildable first-party repositories unresolvable and would remove the source data needed by the approved one-shot importer.",
]
replacement_action = "Keep radroots_event_index, radroots_event_store, radroots_outbox, and radroots_runtime_store as documentation-hidden publish-frozen compatibility packages with no new consumers; port durable behavior into radroots_storage_sqlite through Step 189, remove local transport coupling in Step 196, migrate library/SDK/binding/CLI/indexer consumers in their ordered steps, and delete every remaining package at Step 313."
verification = [
  "Package manifests carry machine-readable publish-frozen metadata naming the final replacement, deviation, Step 313 removal gate, and prohibition on new consumers.",
  "A storage-owned quarantine test requires the four packages to remain private and absent from the approved publication inventory.",
  "Workspace checks and tests prove current migration consumers remain buildable while publication stays frozen.",
  "Step 313 performs the all-first-party forbidden-name search and final package deletion.",
]
unresolved_risk = "None; all four predecessor packages and their active dependency edges are absent."
normative_architecture_change = false
adr_required = false
closure_evidence = [
  "crates/storage_sqlite/tests/package_boundary.rs and crates/storage/tests/workspace_consumers.rs reject the four predecessor package identities.",
  "Step 313 removed the final library packages, SDK runtime/event-index surfaces, and CLI source dependency branch after all standalone consumers migrated.",
]

[[deviation]]
id = "RCRV1-DEV-005"
date = "2026-07-28"
status = "active"
approval = "Explicit user Rust version-policy update dated 2026-07-28 17:47 UTC."
affected_steps = [
  "013",
  "019",
  "020",
  "021",
  "022",
  "023",
  "024",
  "025",
  "026",
  "305",
]
spec_anchors = [
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#repositories.lib",
]
source_evidence = [
  "The prior 1.0.0 and mixed 0.1.0 prerelease crate cohorts were explicitly declared incorrect.",
  "The user requires every Rust crate in radrootslabs/lib to remain exactly 0.1.0-alpha until further explicit notice.",
]
replacement_action = "Pin every workspace package, lockfile entry, and internal Radroots dependency requirement in radrootslabs/lib to 0.1.0-alpha; preserve independent protocol and sibling-repository versions; reject library cohort drift until new explicit authority is recorded."
verification = [
  "Repository architecture validation rejects any workspace package version other than 0.1.0-alpha.",
  "Internal Radroots dependency requirements resolve exactly to =0.1.0-alpha.",
  "Synchronized release specifications record the library cohort independently from the SDK repository version.",
]
unresolved_risk = "The prerelease cohort intentionally prevents independent library package version advancement until a future explicit policy change."
normative_architecture_change = false
adr_required = false

[[deviation]]
id = "RCRV1-DEV-002"
date = "2026-07-27"
status = "active"
approval = "Explicit user correction dated 2026-07-27."
affected_steps = ["249"]
spec_anchors = [
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots",
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#repositories.sdk",
]
source_evidence = [
  "The final v1 specification assigns the radroots facade to the existing sdk repository.",
  "The approved sequence requires radroots to be the first crate-surface mutation after architecture controls are green.",
]
replacement_action = "Scaffold radroots in the sdk repository immediately after Step 014, then execute Steps 250-260 in their original order without repeating the scaffold portion of Step 249."
verification = [
  "The sdk release policy reserves radroots as an approved local package while publication remains frozen.",
  "The facade scaffold checkpoint must add radroots only to the sdk workspace and architecture policy.",
]
unresolved_risk = "The facade remains non-publishable until the package-realistic Step 305 enablement gate."
normative_architecture_change = false
adr_required = false

[[deviation]]
id = "RCRV1-DEV-004"
date = "2026-07-28"
status = "active"
approval = "Explicit user coverage-policy update dated 2026-07-28 17:15 UTC."
affected_steps = [
  "098",
  "155",
  "225",
  "260",
  "268",
  "294",
  "298",
  "299",
  "301",
  "302",
  "303",
  "304",
  "314",
]
spec_anchors = [
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#quality_policy.coverage",
]
source_evidence = [
  "The oss/lib codebase is under heavy development during the multi-RCLD refactor.",
  "The user explicitly replaced the active 100% coverage requirement with a uniform 90% module requirement.",
]
replacement_action = "Enforce 90% executable-line, function, region, and branch coverage for every required oss/lib crate; retain only no-branch-record exceptions; defer restoration of 100% until an explicit future contract update after refactor stabilization."
verification = [
  "Contract validation rejects any base coverage dimension other than 90% or disabled required branches.",
  "Coverage policy-gate tests prove values below 90% fail and values at or above 90% pass.",
  "The required-crate inventory remains complete and crate-specific numeric thresholds below 90% remain forbidden.",
]
unresolved_risk = "A 90% development gate admits untested paths that a later 100% gate would reject; the final restoration remains intentionally unscheduled pending explicit authority."
normative_architecture_change = false
adr_required = false

[[deviation]]
id = "RCRV1-DEV-011"
date = "2026-08-03"
status = "closed"
approval = "All code-review recommendations and the full multi-RCLD implementation sequence were explicitly approved by the user."
affected_steps = ["225", "226", "248"]
spec_anchors = [
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_geonames",
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_sdk",
]
source_evidence = [
  "The standalone SDK manifest and GeoNames module still resolve radroots_geocoder and its test-fixture feature while the final provider package is implemented in this independently versioned repository.",
  "The legacy package has no in-repository consumer, is publish disabled, and is already excluded from the exact release-v1 public package inventory.",
  "Deleting the package at Step 225 would make the independently buildable SDK repository unresolvable before its ordered manifest and API cutover begins at Step 226.",
]
replacement_action = "Keep radroots_geocoder as a documentation-marked, machine-classified publish-frozen bridge with no new consumers, features, contracts, or behavior; migrate the standalone SDK to radroots_geonames beginning at Step 226 and delete the bridge at the SDK retirement gate in Step 248."
verification = [
  "The predecessor manifest names radroots_geonames as its replacement, RCRV1-DEV-011 as authority, and Step 248 as the exact removal gate.",
  "GeoNames package quarantine tests require the predecessor to remain private and absent from the approved publication inventory while the SDK source census remains non-empty.",
  "Step 248 must reject every remaining package, dependency, feature, import, and error-adapter reference to radroots_geocoder before deletion.",
]
unresolved_risk = "None; the SDK consumes radroots_geonames and the predecessor package is absent."
normative_architecture_change = false
adr_required = false
closure_evidence = [
  "crates/geonames/tests/package_boundary.rs rejects the superseded package directory and release-policy identity.",
  "The SDK Step 248 checkpoint removed every radroots_geocoder dependency, feature, import, and error-adapter reference.",
]

[[deviation]]
id = "RCRV1-DEV-012"
date = "2026-08-03"
status = "closed"
approval = "All code-review recommendations and the full multi-RCLD implementation sequence were explicitly approved by the user."
affected_steps = ["279", "282", "283"]
spec_anchors = [
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_transport",
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_transport_nostr",
  "contracts/crates/release_v1/radroots_crates_release_v1.toml#package.radroots_sdk",
]
source_evidence = [
  "The first-party mobile compile gate proved that its real relay-backed identity, profile, and post operations had no equivalent after the initial shared-engine bridge cutover.",
  "The generic source request could bound pages and targets but could not express event kind, author, or event-time constraints required for correct profile and feed queries.",
  "Restoring the retired parallel runtime or accepting client-side filtering after page truncation would violate the final SDK and transport ownership model.",
]
replacement_action = "Before qualifying the mobile artifact, add bounded transport-neutral fetch selectors, translate them in the concrete Nostr adapter, complete SDK-owned explicit local-signing and Nostr composition, and map the mobile presentation contract over those SDK operations without restoring direct lower-package dependencies."
verification = [
  "Transport selector construction rejects oversized, duplicate, and reversed-range inputs and binds selectors into page validation.",
  "The Nostr adapter applies kind, author, and time constraints remotely and defensively filters returned events before page bounds.",
  "The shared-engine SDK and mobile integration gates must pass before Steps 282 and 283 are marked complete.",
]
unresolved_risk = "None; the shared-engine and mobile qualification checkpoints completed with the final selector and SDK ownership model."
normative_architecture_change = false
adr_required = false
closure_evidence = [
  "crates/transport/tests/source_contract.rs verifies bounded canonical selector construction and request binding.",
  "crates/transport_nostr/src/source.rs tests remote selector translation and defensive result filtering before page bounds.",
  "Steps 282 and 283 qualified the SDK-owned signing, Nostr composition, and mobile presentation bridge without lower-package ownership leakage.",
]
